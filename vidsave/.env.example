# Base URL of the VidSave API.
# Leave unset for local development - the Vite dev server proxies /api to
# http://localhost:5000 (see vite.config.js).
# Set this when the frontend and API are deployed to different origins.
VITE_API_URL=https://your-api.up.railway.app
